name: CI Build and push Image 

on:
    push:
        branches: [feauture/ec2-infra-deploy, main]
    pull_request:
        branches: [main]

env:
    AWS_REGION: us-east-1        

    jobs:
        build:
            runs-on: ubuntu-latest

            